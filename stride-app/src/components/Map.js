import React from 'react';
import logo from '../images/react.png';

const Map = () => {
    return(
        <div id="map">
            Map
            <img src={logo} alt='React'></img>
        </div>
    );
}

export default Map;